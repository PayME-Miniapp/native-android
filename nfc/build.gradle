apply plugin: 'maven-publish'

configurations.maybeCreate("default")
artifacts.add("default", file('nfc.aar'))

publishing {
    publications {
        aar(MavenPublication) {
            groupId = "com.payme.sdk"
            artifactId = "nfc"
            version = '0.2.0-alpha.16'

            artifact(file('nfc.aar')) {
                extension 'aar'
            }
        }
    }
}