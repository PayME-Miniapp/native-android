configurations.maybeCreate("default")
artifacts.add("default", file('nfc.aar'))

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = "com.payme.sdk"
                artifactId = "nfc"
                version = '0.2.0-alpha.5'
            }
        }
    }
}
